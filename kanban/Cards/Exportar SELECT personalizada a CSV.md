Implementar también una funcionalidad para que la vista de la base de datos que se está mostrando se pueda exportar a un archivo CSV, que tenga una estructura similar al archivo CSV original.

La primera línea debe contener un encabezado con los nombres de los campos que se han seleccionado, separados por el carácter “;” y las líneas siguientes los valores de los registros que hayas mostrado (también separados por “;”). 

Debes tener en cuenta que el total de campos va a depender de lo que hayas puesto en la consulta SELECT.